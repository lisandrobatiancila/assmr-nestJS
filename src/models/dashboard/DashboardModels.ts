export interface DashboardGetGraphsModel {
  totalVehicle: number; // all-even posted property
  totalJewelry: number; // all-even posted property
  totalRealestate: number; // all-even posted property
}
